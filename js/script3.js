// Using const keyword

const address="bagicha no 05 city road neemuch"; //Define and initalize the Variable
const friend="Aadil";  //Define and initalize the Variable
const mobile="786999"

console.log(address);
console.log(friend);
console.log(mobile);